<main class="text-center form-signin">
  <div style="margin-top:100px;">
    <h2 class="mt-4 mb-5 head-line title_h2 fas fa-sign-in-alt">ログイン</h2>
  </div>
  <div class="container">
    <div class="row" style="margin-bottom:100px;">
      <div class="mx-auto" style="width: 600px;">
        <div class="border border-secondary rounded bg-light text-dark" style="padding:10px;">
          <%= form_with model: @user, url: user_session_path, local: true do |f| %>
            <div class="field pb-2">
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control', placeholder: 'メールアドレス' %>
            </div>
          
            <div class="field pb-4">
              <%= f.password_field :password, autocomplete: "current-password", class: 'form-control', placeholder: 'パスワード' %>
            </div>
          
            <div class="actions pb-3">
              <%= f.submit "ログイン", class: 'btn btn-outline-success btn-lg' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <div class="text-center">
    <h4 class="mt-5 mb-3 head-line title_h4 fas fa-sign-in-alt fas fa-user-plus">
      <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
        <%= link_to "新規登録", new_registration_path(resource_name) %>
      <% end %>はこちら
    </h4>
  </div>


